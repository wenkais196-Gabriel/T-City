-- ============================================================================
-- garbage_collection.json — 垃圾回收工作 JSON 配置模板
-- ============================================================================
-- 将此文件放到 configs/ 目录, 配合 atom_nodes + core_economy 使用。
-- 原 qb-garbagejob 的所有硬编码参数全部提取到此。
--
-- 使用方式:
--   服务端读取本文件 → 生成路线 → 每个站点 = 一个 GOTO 节点
--   完成全部站点后 → exports['core_economy']:TriggerReward() 发放奖励
-- ============================================================================

return {
    id = 'garbage_collection',
    label = 'Garbage Collection Route',
    category = 'civilian_job',
    heatActivityId = 'garbage_collection',

    -- 经济参数 (从 economy_baseline.json 的 civilian_grind=$1500/h 自动推导)
    economy = {
        bagWorthMin = 8,    -- 每袋垃圾最低价值
        bagWorthMax = 15,   -- 每袋垃圾最高价值
        truckDeposit = 250, -- 卡车押金
    },

    -- 路线生成参数
    routeGeneration = {
        minStops = 3,
        maxStops = 8,
        minBagsPerStop = 2,
        maxBagsPerStop = 6,
    },

    -- 采集站点 (配置 — 不是代码!)
    trashCans = {
        { coords = { x = -345.0, y = -1550.0, z = 25.2 } },
        { coords = { x = -320.0, y = -1520.0, z = 27.5 } },
        { coords = { x = -310.0, y = -1480.0, z = 30.5 } },
        { coords = { x = -350.0, y = -1450.0, z = 31.2 } },
        { coords = { x = -400.0, y = -1500.0, z = 28.7 } },
        { coords = { x = -370.0, y = -1540.0, z = 26.9 } },
        { coords = { x = -280.0, y = -1510.0, z = 27.4 } },
        { coords = { x = -250.0, y = -1470.0, z = 31.1 } },
        { coords = { x = -390.0, y = -1430.0, z = 30.9 } },
        { coords = { x = -430.0, y = -1480.0, z = 27.0 } },
    },

    -- 奖励 (走统一网关)
    reward = {
        money = { bank = 0 }, -- 动态计算: bagWorth × bags
        rep = { garbage = 1 },
    },

    -- 随机掉落
    randomDrop = {
        item = 'cryptostick',
        chance = 5,  -- 5%
    },
}
