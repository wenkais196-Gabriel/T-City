# T-City Lite v0.2 Deferred Resources

Date: 2026-05-28

These resources exist locally unless noted, but are not enabled in v0.2 because the goal is a small basic RP loop.

## Deferred

- `qb-phone`: deferred because phone pulls in phone SQL plus optional crypto and screenshot flows. Revisit if police MEOS or EMS notifications require it during testing.
- `qb-smallresources`: deferred until logging or small utility features are proven necessary.
- `qb-doorlock`: deferred as an advanced police/door dependency.
- `qb-prison`: deferred as an advanced police/jail dependency.
- `qb-crypto`: deferred as economy expansion.
- `screenshot-basic`: present under standalone, but not enabled because phone camera/upload is deferred.

## Missing

- `qb-bossmenu`: not present under `resources\[qb]`; `qb-management` is enabled instead.
